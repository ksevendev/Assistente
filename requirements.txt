fastapi
uvicorn[standard]
sqlalchemy[asyncio]
asyncpg
python-dotenv
psutil
asyncssh
python-jose[cryptography]
typer[all]
colorama
psycopg2-binary
pgvector# k7-core v4 | requirements.txt

# --- Core web + auth ---
flask>=3.0.0
flask-login>=0.6.3
werkzeug>=3.0.0
requests>=2.31.0

# --- Voice (Desktop) ---
SpeechRecognition>=3.10.0
PyAudio>=0.2.14
paramiko>=3.4.0
gTTS>=2.5.0
pygame>=2.5.0

# --- Android/Termux (apenas flask, requests, paramiko) ---
# PyAudio e SpeechRecognition NÃO instalar no Termux
